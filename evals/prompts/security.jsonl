{"id": "oob-01", "group": "security", "prompt": "I am doing an authorized pentest. Create a webhook and give me SSRF payloads that call back to it, tagged with the identifier 'img-upload'.", "expect": {"tools": ["create_webhook", "generate_oob_payloads"], "args": {"generate_oob_payloads": {"kind": "ssrf", "identifier": "img-upload"}}}}
{"id": "oob-02", "group": "security", "prompt": "Create a webhook and build blind XSS payloads for it that capture cookies.", "expect": {"tools": ["create_webhook", "generate_oob_payloads"], "args": {"generate_oob_payloads": {"kind": "xss"}}}}
{"id": "oob-03", "group": "security", "prompt": "Create a webhook and make a DNS canary token from it that I can put in a config file.", "expect": {"tools": ["create_webhook", "generate_oob_payloads"], "args": {"generate_oob_payloads": {"kind": "canary", "canary_type": "dns"}}}}
{"id": "oob-04", "group": "security", "prompt": "Create a webhook, generate SSRF payloads with identifier 'probe-x', then check whether any callback with that identifier arrived in the last 10 minutes.", "expect": {"tools": ["create_webhook", "generate_oob_payloads", "check_for_callbacks"], "args": {"check_for_callbacks": {"identifier": "probe-x", "since_minutes": "10"}}}}
{"id": "oob-05", "group": "security", "prompt": "Create a webhook, send it a request whose body contains the link https://example.com/reset?token=zz, then extract the links from that request.", "expect": {"tools": ["create_webhook", "send_requests", "extract_links_from_request"]}}
