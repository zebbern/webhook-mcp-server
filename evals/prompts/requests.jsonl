{"id": "capture-01", "group": "requests", "prompt": "Create a webhook, send it a test JSON payload {\"order\": 42} and show me what was captured.", "expect": {"tools": ["create_webhook", "send_requests"], "args": {"send_requests": {"order": "42"}}}}
{"id": "capture-02", "group": "requests", "prompt": "Create a webhook and send it five different test payloads spaced 200 ms apart, then tell me how many arrived.", "expect": {"tools": ["create_webhook", "send_requests"], "args": {"send_requests": {"payloads": ".", "delay_ms": "200"}}}}
{"id": "capture-03", "group": "requests", "prompt": "Create a webhook, send it a PUT request with body {\"k\":1}, then show only the PUT requests it has received.", "expect": {"tools": ["create_webhook", "send_requests", "search_requests"], "args": {"send_requests": {"method": "(?i)put"}, "search_requests": {"query": "(?i)method:put"}}}}
{"id": "capture-04", "group": "requests", "prompt": "Create a webhook, send a request to it, then give me the newest captured request including its raw body exactly as received.", "expect": {"tools": ["create_webhook", "send_requests", "get_request"], "args": {"get_request": {"raw": "true"}}}}
{"id": "capture-05", "group": "requests", "prompt": "Create a webhook, send it three requests, then export everything it captured as CSV.", "expect": {"tools": ["create_webhook", "send_requests", "export_webhook_data"], "args": {"export_webhook_data": {"format": "csv"}}}}
{"id": "capture-06", "group": "requests", "prompt": "Create a webhook, send it a request, then attach the note 'checked by QA' to that request.", "expect": {"tools": ["create_webhook", "send_requests", "update_request"], "args": {"update_request": {"note": "checked by QA"}}}}
{"id": "capture-07", "group": "requests", "prompt": "Create a webhook, send it two requests, then delete every request on it that is older than 7 days.", "expect": {"tools": ["create_webhook", "send_requests", "delete_all_requests"], "args": {"delete_all_requests": {"date_to": "now-7d"}}}}
{"id": "capture-08", "group": "requests", "prompt": "Create a webhook, send it a request, then delete that single request and confirm the webhook is empty.", "expect": {"tools": ["create_webhook", "send_requests", "delete_request"]}}
{"id": "cleanup-01", "group": "requests", "prompt": "Create a webhook and then delete it again; confirm it is gone.", "expect": {"tools": ["create_webhook", "delete_webhook"]}}
