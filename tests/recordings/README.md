# Recordings

Real exchanges with webhook.site, captured by `scripts/live_tool_check.py --record` and `scripts/verify_action_types.py`, replayed by `tests/test_replay_recordings.py`. What each file holds, how personal data is replaced and when to re-record: [docs/testing.md](../../docs/testing.md), section 2.
